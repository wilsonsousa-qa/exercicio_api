# Primeira API

Este é um projeto de demonstração para o Spring Boot. Ele foi criado para ilustrar como criar uma API RESTful simples usando o Spring Boot e o Maven.

## Pré-requisitos

- Java 11
- Maven

## Como executar

Para executar este projeto, siga os passos abaixo:

1. Navegue até o diretório do projeto usando o comando `cd`:

```bash
cd caminho/para/o/diretório/primeiraApi
